
	<div class="post card">
		<div class="card-header">
		<h2>Edit Your Talk</h2>
		</div>

		<form id="edit-post-form" class="card-body">
			<input type="hidden" name="post-id" value="{{post.id}}"/>

		<label class="form-label" for="post-title">Title</label>
		<input type="text" name="post-title" value="{{post.title}}" class="form-input" />

		<label class="form-label" for="post-body">Talk</label>
		<textarea name="post-body" class="form-input">{{post.body}}</textarea>

		<ul style="list-style: none;">
		<li><button type="submit" class="update-btn">👉 Update Your Talk</button></li>

		<li><button type="submit" id="delete-btn" class="delete-btn">👉 Delete Your Talk</button></li>
		</ul>
		</form>
	</div>

	{{#if loggedIn}}
	<script src="/js/edit.js"></script>
	{{/if}}